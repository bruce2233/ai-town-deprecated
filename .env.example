# AI Town Configuration

# --- LLM Provider Configuration ---
# The URL of your LLM provider (e.g., local Qwen, Ollama, or OpenAI)
# For Mac/Windows Docker Desktop to access host: http://host.docker.internal:8082/v1
# For Linux or LAN: http://192.168.x.x:8082/v1
LLM_PROVIDER_URL=https://api.openai.com/v1

# API Key for the LLM provider (default: dummy)
LLM_PROVIDER_KEY=sk-dummy

# --- Agent Configuration ---
# The model name to request from the provider (must match what your provider serves)
MODEL_NAME=Qwen/Qwen2.5-72B-Instruct

# --- Optional Fallbacks ---
# OpenAI API Key (used if the custom provider fails or for specific tasks)
OPENAI_API_KEY=sk-dummy
